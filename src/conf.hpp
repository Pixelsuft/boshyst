#pragma once

namespace conf {
	extern bool no_vp;
	extern bool god;
	extern bool menu;
	extern bool keep_save;
	extern bool no_cmove;
	extern bool draw_cursor;
	extern int pos[2];
	extern int size[2];

	void read();
}
